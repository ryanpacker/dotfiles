# Managed by chezmoi. Machine-to-machine SSH-mesh Host blocks are generated by
# `ssh-fleet-sync` (from the private r9os fleet registry) into ~/.ssh/fleet.conf,
# Include'd below. Run `ssh-fleet-sync` after r9os is first cloned or updated.
# (ssh silently ignores a missing Include, so this is safe before the first sync.)
Include ~/.ssh/fleet.conf
