tap "symfony-cli/tap"
tap "teamookla/speedtest"

# CLI tools
brew "chezmoi"
brew "gh"
brew "git"
brew "node"
brew "teamookla/speedtest/speedtest"
brew "mas"
brew "dockutil"
brew "defaultbrowser"

# Dev tools
brew "pyenv"
brew "php"
brew "postgresql@16"
brew "rabbitmq"
brew "symfony-cli/tap/symfony-cli"

# Fonts
cask "font-source-code-pro"
cask "font-jetbrains-mono"

# Apps — all machines
cask "1password"
cask "adobe-creative-cloud"
cask "autodesk-fusion"
cask "bambu-studio"
cask "claude"
cask "cursor"
cask "dropbox"
cask "ghostty"
cask "google-earth-pro"
cask "obs"
cask "prusaslicer"
cask "t3-code"
cask "visual-studio-code"
cask "wispr-flow"
# Note: Claude Code intentionally NOT installed here. See
# run_once_after_15-install-claude-code.sh for why.
# Note: Canon Professional Print & Layout has no Homebrew cask — install manually.

{{ if not .isBHR -}}
# Apps JAMF deploys on BHR machines — skip there to avoid version conflicts.
# Edit this group as you verify what JAMF installs for your role.
cask "google-chrome"
cask "google-drive"
cask "microsoft-office"
cask "slack"
cask "zoom"
{{ end -}}

# Mac App Store apps. Requires being signed in to the App Store first (see the
# Pre-Chezmoi Prerequisites section of SETUP.md). `brew bundle` runs `mas install`
# under the hood, which fails if not signed in.
mas "Speedtest", id: 1153157709
mas "Blackmagic Disk Speed Test", id: 425264550
